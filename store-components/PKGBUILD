targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-store-components"
pkgver="3.0.2"
pkgrel="1"
pkgdesc="Carbonio components for store package"
pkgdesclong=(
  "Carbonio store components pulls in all the packages used by"
  "carbonio-store"
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-store-base"
  "carbonio-mariadb"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
