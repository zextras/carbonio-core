targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-ldap-base"
pkgver="3.0.0"
pkgrel="1"
pkgdesc="Carbonio LDAP base"
pkgdesclong=(
    "Carbonio LDAP base is used as a simple method to allow removing"
    "all the LDAP specific packages"
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-base"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
