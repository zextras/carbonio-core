targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-store-base"
pkgver="3.0.0"
pkgrel="1"
pkgdesc="Carbonio store base"
pkgdesclong=(
    "Carbonio store base is used as a simple method to allow removing"
    "all the store specific packages."
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-base"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
