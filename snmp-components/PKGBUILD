targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-snmp-components"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="Carbonio components for snmp package"
pkgdesclong=(
  "Carbonio snmp components pulls in all the packages used by"
  "carbonio-snmp"
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-snmp-base"
  "carbonio-net-snmp"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
