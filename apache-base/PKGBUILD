targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-apache-base"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="Carbonio apache base"
pkgdesclong=(
    "Carbonio apache base is used as a simple method to allow removing"
    "all the apache specific packages"
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-base"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
