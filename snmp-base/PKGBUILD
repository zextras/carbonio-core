targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-snmp-base"
pkgver="21.00"
pkgrel="1"
pkgdesc="Carbonio SNMP base"
pkgdesclong=(
  "Carbonio snmp base is used as a simple method to allow removing"
  "all the SNMP specific packages."
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-base"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
