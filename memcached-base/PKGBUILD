targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-memcached-base"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="Carbonio memcached Base"
pkgdesclong=(
    "Carbonio memcached Base is used as a simple method to allow removing"
    "all the zextras-memcached specific packages."
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-base"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
