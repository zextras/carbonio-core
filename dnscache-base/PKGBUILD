targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-dnscache-base"
pkgver="1.0.0"
pkgrel="1"
pkgdesc="Carbonio dnscache Base"
pkgdesclong=(
    "Carbonio dnscache Base is used as a simple method to allow removing"
    "all the zextras-dnscache specific packages."
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-base"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
