targets=(
  "centos"
  "ubuntu"
)
pkgname="carbonio-proxy-components"
pkgver="21.00"
pkgrel="1"
pkgdesc="Carbonio components for proxy package"
pkgdesclong=(
  "Carbonio proxy components pulls in all the packages used by"
  "carbonio-proxy"
)
arch="amd64"
maintainer="Zextras <packages@zextras.com>"
url="https://zextras.com"
depends=(
  "carbonio-proxy-base"
  "carbonio-memcached"
  "carbonio-nginx"
)
depends:apt=(
  "sqlite3"
)
depends:yum=(
  "sqlite"
)
section="mail"
priority="optional"

package() {
  cd "${srcdir}"/../../
}
